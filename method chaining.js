let userName="Vikram reddy";
let letter=userName.charAt(5).toUpperCase().trim();
console.log(letter);
//when we want to execute a method after another in a continuous execution then we can use method chaining.
let my_Name=userName.slice(0,6).toUpperCase().trim();
console.log(my_Name);